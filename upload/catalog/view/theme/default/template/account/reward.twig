{{ header }}
<script
      src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"
      integrity="sha512-CNgIRecGo7nphbeZ04Sc13ka07paqdeTu0WR1IM4kNcpmBAUSHSQX0FslNhTDadL4O5SAGapGt4FodqL8My0mA=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    ></script>
<div id="account-reward" class="container">
  <ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
    {% endfor %}
  </ul>
  <div class="row">{{ column_left }}
    {% if column_left and column_right %}
    {% set class = 'col-sm-6' %}
    {% elseif column_left or column_right %}
    {% set class = 'col-sm-9' %}
    {% else %}
    {% set class = 'col-sm-12' %}
    {% endif %}
    <div id="content" class="{{ class }}">{{ content_top }}
      <h1>{{ heading_title }}</h1>
      <p>{{ text_total }} <b>{{ total }}</b>.</p>
      <div class="table-responsive">
        <table class="table table-bordered table-hover">
          <thead>
            <tr>
              <td class="text-left">{{ column_date_added }}</td>
              <td class="text-left">{{ column_description }}</td>
              <td class="text-right">{{ column_points }}</td>
            </tr>
          </thead>
          <tbody>
          
          {% if rewards %}
          {% for reward in rewards %}
          <tr>
            <td class="text-left">{{ reward.date_added }}</td>
            <td class="text-left">{% if reward.order_id %} <a href="{{ reward.href }}">{{ reward.description }}</a> {% else %}
              {{ reward.description }}
              {% endif %}</td>
            <td class="text-right">{{ reward.points }}</td>
          </tr>
          {% endfor %}
          {% else %}
          <tr>
            <td class="text-center" colspan="3">{{ text_empty }}</td>
          </tr>
          {% endif %}
            </tbody>
          
        </table>
      </div>
      <div class="row">
        <div class="col-sm-6 text-left">{{ pagination }}</div>
        <div class="col-sm-6 text-right">{{ results }}</div>
      </div>
      <div class="row">
        <div class="col-sm-12">
          當你到店舖消費時，可出示此二維碼，店員會幫你掃描，即可累積積分或使用積分。
          <div style="background: rgb(201,173,198); background: linear-gradient(142deg, rgba(201,173,198,1) 10%, rgba(228,204,225,1) 36%, rgba(201,173,198,1) 43%, rgba(228,204,225,1) 90%); width: 400px; height: 250px; border-radius: 10px;box-shadow: 12px 10px 25px 2px rgba(0,0,0,0.52);
-webkit-box-shadow: 12px 10px 25px 2px rgba(0,0,0,0.52);
-moz-box-shadow: 12px 10px 25px 2px rgba(0,0,0,0.52); position: relative;">
            <img src="https://shop.newidolgroup.com/image/catalog/NIG-logo outline-01.png" style="width: 100px; height: 100px;margin-left:10px;" />
            <div id="qrcode" style="position: absolute; right: 20px; bottom: 20px"></div>
          </div>
        </div>
      </div>
      <div class="buttons clearfix">
        <div class="pull-right"><a href="{{ continue }}" class="btn btn-primary">{{ button_continue }}</a></div>
      </div>
      {{ content_bottom }}</div>
    {{ column_right }}</div>
</div>
<script>
  $(document).ready(function() {
    const qrcode = new QRCode(document.getElementById("qrcode"), {
      text: '{{ userid }}',
      width: 150,
      height: 150,
    }); 

  })
</script>
{{ footer }}